{{-- @extends('layouts.header')

@section('content')
    <div class="container mx-auto">
        <div class="flex justify-center mt-6">
            <div class="w-full max-w-lg">
                <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
                    <div class="mb-4">
                        <h1 class="text-2xl font-bold">Détails de la réservation</h1>
                    </div>
                    <div class="mb-4">
                        <label class="block text-gray-700 font-bold mb-2">Chambre :</label>
                        <p>{{ $reservation->chambre->numero }}</p>
                    </div>
                    <div class="mb-4">
                        <label class="block text-gray-700 font-bold mb-2">Email :</label>
                        <p>{{ $reservation->email }}</p>
                    </div>
                    <div class="mb-4">
                        <label class="block text-gray-700 font-bold mb-2">Date d'arrivée :</label>
                        <p>{{ $reservation->date_arrivee }}</p>
                    </div>
                    <div class="mb-4">
                        <label class="block text-gray-700 font-bold mb-2">Date de départ :</label>
                        <p>{{ $reservation->date_depart }}</p>
                    </div>
                    <div class="flex items-center justify-center mt-6">
                        <a href="{{ route('admin.reservations.index') }}" class="bg-gray-700 text-white py-2 px-4 rounded hover:bg-gray-600">Retour</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection --}}
